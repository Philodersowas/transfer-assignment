# Strengths
1. using `git` you can keep track of changes made to file and e.g. revert to previous versions if necessary (it basically allows for time travel ;) )
2. `git` provides a very useful tool for collaborative projects, by pulling and merging changes of multiple people

# Weaknesses
1. `git` is not optimal for large files and other formats like `pdf`, etc.
2. Storing the entire history of a file does take up more space, though it is done efficiently